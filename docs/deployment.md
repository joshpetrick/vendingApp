# Deployment

Deploy `snackledger-server` to a network-accessible host and configure HTTPS for production. Build `snackledger-kiosk` as its own executable JAR for the scanner/touchscreen workstation.
